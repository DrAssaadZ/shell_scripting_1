func()
{
	echo "hello"
}
func2()
{
	echo "my name is slim shady"
}
#in the terminal `chmod +x func.sh` to turn the file into an execytable file
#then `. func.sh` to execute the file, now the functions has been added to the system
#to execute the func() just type func in terminal
